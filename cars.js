document.getElementById('sort').addEventListener('change', function() {
    const sortValue = this.value;
    // You can add logic here to sort the car cards based on the selection
    alert('You selected: ' + sortValue);
});
